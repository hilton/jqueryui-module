# Application dependencies

require:
   - play
   - local -> jqueryui 0.1

# Modules in the same directory as the application.
repositories:
   - Custom_Modules:
      type: local
      artifact: "${application.path}/../[module]-[revision]"
      contains:
         - local -> *
